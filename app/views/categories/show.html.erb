<section class="categories-show">

  <header class="page-header">
    <h1>
      <%= @category.name %>
      <small>(<%= pluralize @category.products.size, 'product' %>)</small>
    </h1>
  </header>

  <div class="products">
    <%= render @category.products %>
  </div>

</section>
